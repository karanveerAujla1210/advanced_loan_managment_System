# SERVER
PORT=5000

# Replace USERNAME and PASSWORD locally on your machine; do NOT commit real credentials
MONGO_URI="mongodb+srv://USERNAME:PASSWORD@cluster0.1ed6kd1.mongodb.net/loancrm?retryWrites=true&w=majority"

# JWT
JWT_SECRET="change_this_to_a_random_long_string"

# Optional: change CORS origin for local dev
CORS_ORIGIN=http://localhost:5173
